# Copy to .env.local and adjust if you're not running the services on the
# default ports from the repo root README.
VITE_AUTH_API_BASE=http://localhost:8001
VITE_ADMIN_API_BASE=http://localhost:8005
