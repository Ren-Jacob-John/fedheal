# --- Installable in this environment / on any lightweight dev machine ---
tabpfn>=2.0          # current-gen default for the vitals specialist — see
                      # docs/model-algorithm-catalog.md Section 4. Pure Python,
                      # no GPU strictly required for vitals-sized tables.
xgboost==3.3.0        # kept as fallback/ensemble partner, still current practice
lightgbm==4.7.0
scikit-learn==1.5.2
numpy==1.26.4
pandas==2.2.3

# --- Needed only for the REAL imaging models in models/imaging_*.py ---
# Not installed in this sandbox (disk-constrained); install on a real dev
# machine or training server before running the imaging specialists for real.
# torch>=2.2
# torchvision>=0.17
# transformers>=4.40  # for BiomedCLIP/MedGemma/CONCH — see catalog Sections 1, 3, 6

# --- Needed only for the current-tier foundation-model specialists added in
# this modernization pass (models/foundation_*.py) — see
# docs/model-algorithm-catalog.md and docs/foundation-models-status.md for
# what's real vs. blocked-here for each. Also needs huggingface.co network
# access (for weights) and a GPU (practically, for all but TabPFN) — none
# available in this sandbox, so every one of these degrades to the legacy
# model or a StubSpecialistModel automatically via registry.py.
# open_clip_torch>=2.24   # BiomedCLIP — imaging_chest_xray/retina/skin upgrade
# nnunetv2>=2.5            # nnU-Net — imaging_segmentation.py upgrade (needs a hospital's own trained model folder)
# evo2                     # Evo 2 — new genomic_variant specialist (needs a supported GPU, per its own README)
